{{#each items}}
	<div class="block">
	    <div id="input-{{slug}}" data-index="{{@index}}" class="btn-select pending">
	        <div class="title">{{name}}</div>
	        <div class="name">&nbsp;</div>
	    </div>
	</div>
{{/each}}
