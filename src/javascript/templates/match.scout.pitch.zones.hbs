<div class="col-sm-12">
    <div class="pitch zones">
        <div class="table">

            {{#zones}}
                <div class="tr">
                    {{#this}}
                        <div class="td zone fnPitchZone fnSelectable fnTap" data-value="{{id}}" data-name="{{name}}">
                            {{name}}
                        </div>
                    {{/this}}
                </div>

            {{/zones}}

        </div>
    </div>
</div>
