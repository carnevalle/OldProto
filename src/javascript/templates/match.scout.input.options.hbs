{{#each items}}
	<div class="col-xs-4">
	    <div class="btn btn-medium btn-square fnValueSelect" data-value="{{id}}" data-name="{{name}}">{{#if number}}{{number}}. {{/if}}{{name}}</div>
	</div>
{{/each}}
